import { Box } from "@mui/joy";

export const headerHeight = "64px";

export default function Header() {
  return (
    <Box sx={{ height: headerHeight }}>
      Hello Header! Header goes here.
    </Box>
  );
}
